import React from 'react'
import ReactDOM from 'react-dom/client'
import AIFashionDesigner from './ai_fashion_designer'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AIFashionDesigner />
  </React.StrictMode>,
)
